name: Deploy Infrastructure

# EDIT ONLY THIS LINE
env:
  TARGET_BRANCH: auth  # Switch to 'hello-world', 'CRUD', or 'auth' as needed.
  AWS_REGION: ap-southeast-2
  TF_VERSION: 1.6.6
  PYTHON_VERSION: "3.12"

on:
  push:
    # This uses a wildcard to trigger on any branch, 
    # but the Job itself will filter based on your TARGET_BRANCH above.
    branches: ["**"] 
  pull_request:
    branches: ["**"]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploying ${{ github.ref_name }}
    runs-on: ubuntu-latest
    # This line ensures the CI only runs if the push matches your TARGET_BRANCH
    if: github.ref_name == vars.TARGET_BRANCH || github.event.inputs.branch == vars.TARGET_BRANCH || contains(github.ref, 'refs/heads/') 
    
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install Lambda dependencies
        run: |
          LAMBDA_DIR="lambda/serverless_web_app"
          if [ -f "$LAMBDA_DIR/requirements.txt" ]; then
            pip install -r "$LAMBDA_DIR/requirements.txt" -t "$LAMBDA_DIR/" --upgrade --no-cache-dir
          fi

      # Automatically runs migrations for CRUD and auth branches.
      - name: Run database migrations
        if: env.TARGET_BRANCH == 'CRUD' || env.TARGET_BRANCH == 'auth'
        working-directory: lambda/serverless_web_app
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DJANGO_SETTINGS_MODULE: config.settings.prod
        run: python manage.py migrate --noinput

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        run: terraform init -migrate-state -force-copy

      - name: Terraform Plan
        id: plan
        env:
          TF_VAR_database_url: ${{ secrets.DATABASE_URL }}
        run: terraform plan -no-color -out=tfplan
        continue-on-error: true

      - name: Post Plan to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const output = `#### Terraform Plan \`${{ steps.plan.outcome }}\`
            <details><summary>Show Plan</summary>
            \`\`\`hcl
            ${{ steps.plan.outputs.stdout }}
            \`\`\`
            </details>
            *Pushed by: @${{ github.actor }}*`;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });

      - name: Check Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Terraform Apply
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        env:
          TF_VAR_database_url: ${{ secrets.DATABASE_URL }}
        run: terraform apply -auto-approve tfplan

      - name: Terraform Output
        if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
        run: |
          echo "========================================"
          echo "    DEPLOYED BRANCH: ${{ env.TARGET_BRANCH }}"
          echo "========================================"
          terraform output -no-color