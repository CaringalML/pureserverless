# Add your Python dependencies here, one per line. Examples:
# requests==2.31.0
# boto3==1.34.0        # only needed if you use a version different from the Lambda runtime's built-in
# pydantic==2.5.0
